# wxctl

- [Introduction](/introduction): `wxctl` takes an opinionated, declarative approach to managing IBM product resources.
- [Installation](/installation): Download and run the install script:
- [Quickstart](/quickstart): Deploy your first agents with `wxctl`. Both quickstarts build the identical `calculator-weather-agents` example (one knowledge base, two Python tools, two agents, four tests) on watsonx Orchestrate and end at the same deployed result. Pick the path that fits how you want to work, or run both and compare.
- [Declarative](/quickstart/declarative): Build your first agents with `wxctl` by creating the `calculator-weather-agents` example from scratch: two Python tools, a knowledge base, and two agents (one delegates to the other). You write every file by hand (each one is copy-paste ready below), then deploy and test it. It needs only a watsonx Orchestrate profile, and both tools are self-contained, so there is nothing external to set up.
- [Generative](/quickstart/generative): Build the same `calculator-weather-agents` example without writing `config.yaml` by hand. You describe the scenario in one sentence, then IBM Bob drives `wxctl mcp serve` to compose the config, scaffold the tools, plan, apply, and test, then tear it all down. The deployed result is identical to the [Declarative](/quickstart/declarative) path: one knowledge base, two Python tools, two agents, and four tests.
- [Declarative model](/concepts/declarative-model): You describe the resources you want in YAML. wxctl figures out the rest: prerequisites, ordering, and the identifier each API expects. You never write an imperative sequence of API calls.
- [Pipeline](/concepts/pipeline): Every command runs the same ordered pipeline. The difference between a dry run and a live change is a single flag deep inside it, not a different code path, so what you preview is what you get.
- [Profiles & credentials](/concepts/profiles-and-credentials): A **profile** is a named set of service endpoints and authentication settings. Commands that talk to remote services (`plan`, `apply`, `destroy`, `test`) read the active profile to know where each service lives and how to authenticate.
- [How wxctl compares](/concepts/how-wxctl-compares): wxctl is a declarative tool for **IBM product resources**, not general infrastructure. It borrows the plan-before-apply ergonomics of infrastructure-as-code but targets a different layer and resolves identifiers differently. Use it alongside an IaC tool, not instead of one.
- [Generate a config from a scenario](/guides/compose): wxctl ships an **MCP orchestrator tool**, `compose_start`, that turns a plain-language scenario into a deployed configuration. Point any MCP client (Claude Code, Cursor, Claude Desktop, or your own agent) at a `wxctl mcp serve` server, and the agent drives the whole flow: identify the resources, generate the YAML, validate and fix it, plan, apply, and test, asking you for anything it can't infer rather than fabricating it.
- [Worked example](/guides/examples): This guide builds a small but complete configuration: a calculator **tool**, an **agent** that calls it, and a **test** that exercises the agent. It mirrors the [declarative model](/concepts/declarative-model) end to end: declare resources, reference them with `${kind.ref_name}`, then run the [pipeline](/concepts/pipeline).
- [Examples gallery](/guides/gallery): Each example is one scenario: a plain-English brief, the `config.yaml` it becomes, and the resources that brief needs. Point `wxctl` at the config to plan, apply, test, and tear it back down. For a hand-authored, step-by-step build, see the [worked example](/guides/examples). To generate a config from a scenario, see the [compose flow](/guides/compose).
- [MCP clients](/guides/mcp-clients): wxctl ships a local [Model Context Protocol](https://modelcontextprotocol.io) server, `wxctl mcp serve`, that exposes its tools to any MCP-capable client over stdio. An agent can discover resource kinds, validate and plan configs, and (with confirmation) apply, test, and destroy them. For config authoring with no local install, point a client at the hosted Worker instead.
- [Use wxctl from an AI agent](/guides/agents): wxctl is a deterministic engine with no LLM of its own: your agent writes the `config.yaml`, and wxctl validates, plans, and executes it. This guide is the runbook for driving the full loop from an agent with only the binary. The same runbook ships in the repository as [`AGENTS.md`](https://github.com/randyphoa/wxctl/blob/main/AGENTS.md).
- [Automation & CI](/guides/automation): wxctl is built for unattended runs. The same config works across environments, re-applying is safe, and every command returns a predictable exit code a pipeline can gate on.
- [Troubleshooting](/guides/troubleshooting): When a run misbehaves, these environment variables expose what wxctl is doing and let you tune how it talks to each service. See [Commands](/reference/commands) for the full command surface and [Pipeline](/concepts/pipeline) for what each stage does.
- [Commands](/reference/commands): {/* Generated by scripts/generate-reference.sh from wxctl 0.1.6. Do not hand-edit; re-run the script to regenerate. */}
- [Resource kinds](/reference/resources): {/* Generated by scripts/generate-reference.sh from wxctl 0.1.6. Do not hand-edit; re-run the script to regenerate. */}
- [AI Factsheets kinds](/reference/kinds/factsheets): {/* Generated by scripts/generate-reference.sh from wxctl 0.1.6. Do not hand-edit; re-run the script to regenerate. */}
- [Cloud Object Storage kinds](/reference/kinds/cloud_object_storage): {/* Generated by scripts/generate-reference.sh from wxctl 0.1.6. Do not hand-edit; re-run the script to regenerate. */}
- [Data & AI Common Core kinds](/reference/kinds/common_core): {/* Generated by scripts/generate-reference.sh from wxctl 0.1.6. Do not hand-edit; re-run the script to regenerate. */}
- [IBM Concert kinds](/reference/kinds/concert): {/* Generated by scripts/generate-reference.sh from wxctl 0.1.6. Do not hand-edit; re-run the script to regenerate. */}
- [IBM Concert Workflows kinds](/reference/kinds/concert_workflows): {/* Generated by scripts/generate-reference.sh from wxctl 0.1.6. Do not hand-edit; re-run the script to regenerate. */}
- [IBM Instana kinds](/reference/kinds/instana): {/* Generated by scripts/generate-reference.sh from wxctl 0.1.6. Do not hand-edit; re-run the script to regenerate. */}
- [IBM Planning Analytics kinds](/reference/kinds/planning_analytics): {/* Generated by scripts/generate-reference.sh from wxctl 0.1.6. Do not hand-edit; re-run the script to regenerate. */}
- [OpenScale kinds](/reference/kinds/openscale): {/* Generated by scripts/generate-reference.sh from wxctl 0.1.6. Do not hand-edit; re-run the script to regenerate. */}
- [watsonx.ai kinds](/reference/kinds/watsonx_ai): {/* Generated by scripts/generate-reference.sh from wxctl 0.1.6. Do not hand-edit; re-run the script to regenerate. */}
- [watsonx.data kinds](/reference/kinds/watsonx_data): {/* Generated by scripts/generate-reference.sh from wxctl 0.1.6. Do not hand-edit; re-run the script to regenerate. */}
- [watsonx Orchestrate kinds](/reference/kinds/watsonx_orchestrate): {/* Generated by scripts/generate-reference.sh from wxctl 0.1.6. Do not hand-edit; re-run the script to regenerate. */}
- [FAQ](/faq): No. Infrastructure-as-code tools like Terraform and Pulumi provision infrastructure: VMs, networks, DNS. wxctl manages **IBM product resources** (AI agents, governance policies, data catalogs, object storage) that span services with incompatible identifier schemes. The two are complementary: IaC stands up the platform, wxctl declares the product resources on it. See [How wxctl compares](/concepts/how-wxctl-compares).
- [Releases](/releases): Releases are published on GitHub. Check the version you are running with:
